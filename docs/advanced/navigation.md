# 高效移动

如果你还在用方向键，那么你还没真正掌握 Vim。

## 基础移动
- `h` `j` `k` `l`
- `w`：移动到下一个单词开头。
- `b`：移动到当前或上一个单词开头。
- `e`：移动到当前或下一个单词结尾。

## 行内快速定位
- `0`：行首。
- `$`：行尾。
- `f{char}`：行内向右查找并跳转到字符 `{char}`。
- `;`：重复上一次 `f` 查找。

## 跨行跳转
- `gg`：跳到文件开头。
- `G`：跳到文件末尾。
- `:{num}`：跳到指定行。
- `%`：在配对的括号 `()` `[]` `{}` 之间跳转。
